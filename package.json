{
  "name": "whitebox",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test --experimental-strip-types test/*.test.ts",
    "check": "python3 tools/check-contrast.py && tsc --noEmit",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "astro": "^7.3.1",
    "hash-wasm": "^4.12.0",
    "js-yaml": "^5.4.1",
    "smol-toml": "^1.8.0",
    "typescript": "^7.0.2"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "wrangler": "^4.129.0"
  }
}
